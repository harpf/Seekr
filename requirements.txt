pypdf>=4.0.0
python-docx>=1.1.0
python-pptx>=0.6.23
PyYAML>=6.0.1,<7.0
fastapi>=0.115.0
uvicorn>=0.32.0
jinja2>=3.1.4,<4.0
pillow>=10.0.0,<12.0
pytesseract>=0.3.10
pdf2image>=1.17.0
pytest>=8.0.0
python-multipart>=0.0.9,<0.1
python-magic>=0.4.27
# Outlook .msg (compound OLE) extraction. .eml uses the stdlib `email` module
# and needs no dependency; .msg has no stdlib parser. The import is lazy and
# tolerant — a missing wheel degrades to a per-file error, never an import crash.
extract-msg>=0.48.0

starlette==0.50.0
cryptography>=42.0.0,<46.0
psutil>=5.9.0
prometheus-client>=0.20.0

# Optional: enable the Redis backend for sessions + rate limiting in
# multi-replica deployments. Activate with DOCUMENT_SEARCH_STATE_BACKEND=redis.
# Not required for the default SQLite backend (used in CI).
# redis>=5.0.0
